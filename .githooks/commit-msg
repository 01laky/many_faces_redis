#!/bin/sh
# Second pass: strip Cursor trailers after any late injection.
set -e
root=$(git rev-parse --show-toplevel)
msg_file="$1"
[ -n "$msg_file" ] && [ -f "$msg_file" ] || exit 0
sh "${root}/scripts/strip-cursor-commit-trailers.sh" "$msg_file"
exit 0
